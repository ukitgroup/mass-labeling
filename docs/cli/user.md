# User CLI


## Create user

### Run command

`bin/cli user:create <arguments> | npx bunyan`

#### Arguments

* `--email` - user's email
* `--password` - password
* `--role` - role. Allowed values: `admin`, `user`.
